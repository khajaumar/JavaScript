const obj = {
    name: "khaja",
    city: "Nanded",
    contact: 9856236,
};

// const keys = Object.keys(obj);
// const newArr = keys.map((key) => obj[key]);
// console.log(newArr);

const arr = Object.values(obj);
console.log(arr);